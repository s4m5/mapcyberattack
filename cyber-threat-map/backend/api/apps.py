"""
Конфигурация Django приложения api.
Определяет название приложения и дополнительную логику инициализации.
"""

from django.apps import AppConfig  # Импорт базового класса конфигурации приложения


class ApiConfig(AppConfig):
    """
    Класс конфигурации для приложения api.
    Наследуется от AppConfig и содержит метаданные приложения.
    """
    
    default_auto_field = 'django.db.models.BigAutoField'  # Тип поля по умолчанию для auto-created primary keys
    name = 'api'  # Название приложения (должно совпадать с именем директории)
    verbose_name = 'API Карты Киберугроз'  # Человекочитаемое название приложения
    
    def ready(self):
        """
        Метод вызывается когда приложение готово к работе.
        Здесь можно выполнять дополнительную инициализацию.
        
        Например:
        - Подключение сигналов
        - Запуск фоновых задач
        - Проверка конфигурации
        """
        # Импортируем сигналы если они есть
        # from . import signals
        
        # Выводим сообщение в лог при запуске
        import logging
        logger = logging.getLogger(__name__)
        logger.info('Приложение API Карты Киберугроз успешно инициализировано')
