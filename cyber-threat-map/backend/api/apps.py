"""
Конфигурация приложения Django для карты киберугроз.
Определяет настройки и поведение приложения.
"""

from django.apps import AppConfig  # Импортируем базовый класс конфигурации приложения


class ApiConfig(AppConfig):
    """
    Класс конфигурации для приложения api.
    Наследуется от AppConfig и определяет метаданные приложения.
    """
    
    default_auto_field = 'django.db.models.BigAutoField'  # Тип поля автоинкремента по умолчанию
    name = 'api'  # Имя приложения (должно совпадать с именем пакета)
    verbose_name = 'Карта Киберугроз'  # Человекочитаемое название приложения
    
    def ready(self):
        """
        Метод вызывается когда приложение готово к работе.
        Здесь можно выполнять инициализацию, запуск фоновых задач и т.д.
        """
        # Импортируем сигналы если они есть (для обработки событий модели)
        # from . import signals
        
        # Выводим сообщение о готовности приложения
        print("Приложение 'Карта Киберугроз' успешно загружено")
